import type { RequestHandler } from "express";
export const reviewConsole: RequestHandler = (_req, res) =>
  res.set({"Cache-Control":"no-store","Content-Security-Policy":"default-src 'none'; style-src 'unsafe-inline'; script-src 'unsafe-inline' https://shoo.dev; connect-src 'self' https://shoo.dev; frame-ancestors 'none'; base-uri 'none'; form-action 'self'","Referrer-Policy":"no-referrer","X-Content-Type-Options":"nosniff"}).type("html")
    .send(`<!doctype html><meta charset=utf-8><meta name=viewport content="width=device-width"><title>Field guide reviews</title><script src="https://shoo.dev/shoo.js" defer></script><style>body{font:15px system-ui;max-width:70rem;margin:auto;padding:1rem;background:#0d1117;color:#eee}nav,.actions{display:flex;gap:.5rem;flex-wrap:wrap}button,input{padding:.5rem}article{border:1px solid #444;padding:1rem;margin:1rem 0;border-radius:.6rem}pre{white-space:pre-wrap}.active{background:#58a;color:#000}</style><h1>Field guide reviews</h1><button id=signin>Sign in with Google</button><button id=signout>Sign out</button><p id=status></p><nav><button data-scope=project>Project</button><button data-scope=global>Global</button><button data-view=queue>Pending / due</button><button data-view=history>History</button></nav><main></main><script>
const S={scope:'project',view:'queue',token:null},E=s=>String(s??'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));async function boot(){if(Shoo.parseCallback())await Shoo.handleCallback({redirectTo:'/review'});const i=Shoo.getIdentity();if(i){S.token=i.token;load()}}signin.onclick=()=>Shoo.startSignIn({redirectUri:location.origin+'/review/callback',audience:'origin:'+location.origin});signout.onclick=()=>{Shoo.clearIdentity();location.reload()};document.querySelectorAll('[data-scope]').forEach(b=>b.onclick=()=>{S.scope=b.dataset.scope;load()});document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{S.view=b.dataset.view;load()});async function api(path,opt={}){const r=await fetch(path,{...opt,headers:{Authorization:'Bearer '+S.token,'Content-Type':'application/json'}}),j=await r.json();if(!r.ok)throw Error(j.message);return j}async function load(){const d=await api('/api/review/'+S.view+'?scope='+S.scope);status.textContent='Pending '+d.summary.pending+' · Due '+d.summary.due+' · Overdue '+d.summary.overdue;document.querySelector('main').innerHTML=S.view==='history'?d.decisions.map(x=>'<article><b>'+E(x.title)+'</b><p>'+E(x.action)+' · round '+x.round+'</p></article>').join(''):d.items.map(card).join('');bind()}function card(x){const c=x.candidate,a=x.kind==='initial'?['approve','reject','defer']:['confirm_valid','mark_invalid','defer'];return '<article data-id="'+E(c.candidateId)+'" data-round="'+x.round+'"><small>'+E(c.scope)+' · '+E(x.status)+'</small><h2>'+E(c.title)+'</h2><pre>'+E(c.body)+'</pre><p>'+E(c.rationale)+'</p><details><summary>Evidence</summary>'+c.evidence.map(e=>'<blockquote>'+E(e.excerpt)+'</blockquote><p>'+E(e.sessionRef||'')+' '+e.commitHashes.map(h=>'<code>'+E(h)+'</code>').join(' ')+'</p>').join('')+'</details><div class=actions>'+a.map(v=>'<button data-action="'+v+'">'+v+'</button>').join('')+'<input type=datetime-local></div></article>'}function bind(){document.querySelectorAll('[data-action]').forEach(b=>b.onclick=async()=>{const p=b.closest('article'),body={action:b.dataset.action};if(body.action==='defer')body.deferUntil=new Date(p.querySelector('input').value).toISOString();await api('/api/review/candidates/'+encodeURIComponent(p.dataset.id)+'/rounds/'+p.dataset.round+'/verdict',{method:'POST',body:JSON.stringify(body)});load()})}addEventListener('load',boot);
</script>`);
