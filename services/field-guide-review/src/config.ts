export type Config={port:number;databaseUrl:string;agentApiToken:string;allowedEmail:string;publicBaseUrl:string};
export function loadConfig(env:NodeJS.ProcessEnv=process.env):Config{const get=(name:string)=>{const value=env[name]?.trim();if(!value)throw new Error(`Missing required environment variable: ${name}`);return value;};const publicBaseUrl=new URL(get("PUBLIC_BASE_URL")).origin;const email=get("SHOO_ALLOWED_EMAIL").toLowerCase();if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))throw new Error("SHOO_ALLOWED_EMAIL must be a valid email address");const port=env.PORT?Number(env.PORT):3000;if(!Number.isInteger(port)||port<1||port>65535)throw new Error("PORT must be a valid port");return {port,databaseUrl:get("DATABASE_URL"),agentApiToken:get("AGENT_API_TOKEN"),allowedEmail:email,publicBaseUrl};}
