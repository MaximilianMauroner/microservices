import{describe,expect,it}from"vitest";import{MemoryReviewRepository}from"../src/memory-repository.js";
const id="11111111-1111-4111-8111-111111111111",start=new Date("2026-07-26T00:00:00Z"),candidate={candidateId:id,scope:"project" as const,projectKey:"repo",projectDisplayName:"Repo",lessonKey:"safe",title:'"><img src=x onerror=alert(1)>',body:"Body",rationale:"Why",evidence:[{excerpt:"Evidence",sessionRef:"session",commitHashes:["abc"]}],createdAt:start.toISOString()};
describe("review lifecycle",()=>{it("gates future rounds and preserves 30-day first confirmation across defers",async()=>{const r=new MemoryReviewRepository();await r.createCandidate("key",candidate);const approved=await r.decide(id,1,{action:"approve"},start,"owner@example.com");expect(approved.nextReviewAt).toBe("2026-08-02T00:00:00.000Z");expect(await r.queue(undefined,start)).toHaveLength(0);await expect(r.decide(id,2,{action:"confirm_valid"},start,"owner@example.com")).rejects.toThrow("not due");const due=new Date(approved.nextReviewAt!);const deferred=await r.decide(id,2,{action:"defer",deferUntil:"2026-08-05T00:00:00Z"},due,"owner@example.com");const confirmed=await r.decide(id,3,{action:"confirm_valid"},new Date(deferred.nextReviewAt!),"owner@example.com");expect(confirmed.nextReviewAt).toBe("2026-09-04T00:00:00.000Z");});it("returns reviewer, evidence, scoped pagination, and a cursor on the final nonempty page",async()=>{const r=new MemoryReviewRepository();await r.createCandidate("key",candidate);await r.decide(id,1,{action:"approve"},start,"owner@example.com");const page=await r.decisions(undefined,100,"project");expect(page.nextCursor).toBeTruthy();expect(page.decisions[0]).toMatchObject({reviewer:"owner@example.com",evidence:candidate.evidence});expect((await r.decisions(undefined,100,"global")).decisions).toHaveLength(0);});});
