import type{RequestHandler}from"express";import request from"supertest";import{expect,it}from"vitest";import{createApp}from"../src/app.js";import{MemoryReviewRepository}from"../src/memory-repository.js";const pass:RequestHandler=(_q,_r,n)=>n();it("uses configured HTTPS origin instead of untrusted proxy or Host headers",async()=>{const repository=new MemoryReviewRepository(),app=createApp({repository,agentAuth:pass,reviewerAuth:pass,publicBaseUrl:"https://reviews.example",now:()=>new Date("2026-07-26T00:00:00Z")}),candidate={candidateId:"11111111-1111-4111-8111-111111111111",scope:"global" as const,lessonKey:"x",title:"x",body:"x",rationale:"x",evidence:[{excerpt:"x",commitHashes:[]}],createdAt:"2026-07-26T00:00:00Z"};await request(app).post("/api/agent/candidates").send({idempotencyKey:"k",candidate});const path="/api/review/candidates/11111111-1111-4111-8111-111111111111/rounds/1/verdict";expect((await request(app).post(path).set("Host","attacker.example").set("X-Forwarded-Proto","http").set("Origin","https://reviews.example").send({action:"approve"})).status).toBe(201);});
