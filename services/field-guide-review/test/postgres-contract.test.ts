import{readFile}from"node:fs/promises";import{expect,it}from"vitest";it("uses UUID audit columns and conflict-aware immutable inserts",async()=>{const migration=await readFile(new URL("../migrations/001_initial.sql",import.meta.url),"utf8"),source=await readFile(new URL("../src/postgres-repository.ts",import.meta.url),"utf8");expect(migration).toContain("candidate_id uuid PRIMARY KEY");expect(migration).toContain("reviewer text NOT NULL");expect(source.match(/ON CONFLICT DO NOTHING/g)).toHaveLength(2);expect(source).toContain("payload_hash===hash");});
