import{describe,expect,it}from"vitest";import{loadConfig}from"../src/config.js";const base={DATABASE_BACKEND:"sqlite",SQLITE_PATH:"/app/data/field-guide.sqlite",AGENT_API_TOKEN:"secret",SHOO_ALLOWED_EMAIL:"owner@example.com",PUBLIC_BASE_URL:"https://reviews.example"};describe("configuration",()=>{it("accepts SQLite without PostgreSQL",()=>expect(loadConfig(base)).toMatchObject({backend:"sqlite",sqlitePath:"/app/data/field-guide.sqlite",publicBaseUrl:"https://reviews.example"}));it("requires an absolute SQLite path",()=>expect(()=>loadConfig({...base,SQLITE_PATH:"data/review.sqlite"})).toThrow("absolute"));it("supports the temporary PostgreSQL backend",()=>expect(loadConfig({...base,DATABASE_BACKEND:"postgres",DATABASE_URL:"postgres://localhost/test"})).toMatchObject({backend:"postgres",databaseUrl:"postgres://localhost/test"}));it.each(["ftp://reviews.example","https://user:pass@reviews.example","https://reviews.example/path","https://reviews.example/?x=1","https://reviews.example/#x"])("rejects %s",value=>expect(()=>loadConfig({...base,PUBLIC_BASE_URL:value})).toThrow("HTTP(S) origin"));});
