export function renderDropUploadPage(
  expiresAt: Date,
  nonce: string,
  publisherIconUrl: string
) {
  const expiry = expiresAt.toISOString();
  return `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<meta name="robots" content="noindex,nofollow"><title>Send files</title><link rel="icon" href="${publisherIconUrl}" type="image/png" sizes="96x96">
<style nonce="${nonce}">${DROP_UPLOAD_STYLES}</style></head><body><main>
<header><div class="brand"><span class="mark"><img src="${publisherIconUrl}" alt="" width="40" height="40"></span><span>Mauroner Tools</span></div>
<p class="eyebrow">Private upload link</p><h1>Send files</h1>
<p class="intro">Choose as many files as you need and send them securely to the link owner.</p>
<p class="expiry"><span>Available until</span><time datetime="${expiry}" id="expiry">${expiry}</time></p></header>
<form id="form"><label class="drop" id="drop"><svg aria-hidden="true" viewBox="0 0 24 24"><path d="M12 16V4m0 0L7.5 8.5M12 4l4.5 4.5M5 14v4a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2v-4"/></svg><strong>Drop files here</strong><span class="or">or</span><span class="picker">Choose files</span><span class="selection" id="selection">No files selected</span><input id="files" type="file" multiple required></label>
<button id="submit" type="submit" disabled><span>Upload files</span><svg aria-hidden="true" viewBox="0 0 24 24"><path d="M5 12h14m-5-5 5 5-5 5"/></svg></button></form><p id="message" role="status" aria-live="polite"></p><ul id="results"></ul>
<p class="privacy"><svg aria-hidden="true" viewBox="0 0 24 24"><path d="M12 3 5 6v5c0 4.6 2.9 8.8 7 10 4.1-1.2 7-5.4 7-10V6l-7-3Z"/></svg><span>Files go directly to the link owner. Previous uploads are never shown on this page.</span></p>
</main><script nonce="${nonce}">${DROP_UPLOAD_SCRIPT}</script></body></html>`;
}

const DROP_UPLOAD_STYLES = `
:root{font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:#211a2d;background:#f6f3f9;color-scheme:light}*{box-sizing:border-box}body{margin:0;min-width:320px;min-height:100vh;padding:clamp(16px,5vw,56px);background:radial-gradient(circle at 50% -10%,#fff 0,#f8f5fb 42%,#f1ebf6 100%)}main{width:min(680px,100%);margin:0 auto;background:rgba(255,255,255,.96);border:1px solid #ded5e7;border-radius:24px;padding:clamp(24px,6vw,52px);box-shadow:0 28px 80px rgba(52,31,72,.1)}header{display:grid}.brand{display:flex;align-items:center;gap:12px;color:#554760;font-size:14px;font-weight:700}.mark{display:grid;place-items:center;width:56px;height:56px;border:1px solid #e4d9ed;border-radius:17px;background:#f3edf8;box-shadow:inset 0 1px 0 #fff}.mark img{display:block;width:40px;height:40px;object-fit:contain}.eyebrow{margin:32px 0 10px;color:#744a91;font-size:12px;font-weight:800;letter-spacing:.16em;text-transform:uppercase}h1{margin:0;font-size:clamp(40px,8vw,64px);font-weight:750;letter-spacing:-.045em;line-height:.98}.intro{max-width:560px;margin:20px 0 0;color:#665d70;font-size:17px;line-height:1.6}.expiry{display:flex;align-items:center;gap:8px;width:max-content;max-width:100%;margin:22px 0 0;padding:8px 11px;border:1px solid #e7ddec;border-radius:999px;background:#faf8fc;color:#665d70;font-size:13px}.expiry span{font-weight:700}.expiry time{overflow-wrap:anywhere}.drop{position:relative;display:grid;justify-items:center;gap:8px;margin:32px 0 14px;padding:34px 24px;text-align:center;border:2px dashed #c8b6d8;border-radius:18px;background:#fcfafd;cursor:pointer;transition:border-color .16s ease,background .16s ease,transform .16s ease}.drop:hover,.drop.is-over,.drop:has(input:focus-visible){border-color:#7b469d;background:#f7f1fb}.drop.is-over{transform:translateY(-2px)}.drop svg{width:34px;height:34px;margin-bottom:4px;padding:7px;border-radius:10px;background:#eee4f5;color:#6f3d96;fill:none;stroke:currentColor;stroke-linecap:round;stroke-linejoin:round;stroke-width:1.8}.drop strong{font-size:18px}.drop .or{color:#83798b;font-size:13px}.picker{display:inline-flex;align-items:center;justify-content:center;min-height:38px;margin-top:2px;padding:8px 15px;border:1px solid #cfc3d8;border-radius:9px;background:#fff;color:#3e3049;font-size:14px;font-weight:750;box-shadow:0 2px 7px rgba(49,30,66,.07)}.selection{max-width:100%;margin-top:3px;color:#776d80;font-size:13px;overflow-wrap:anywhere}.drop.has-files .selection{color:#5f3679;font-weight:700}.drop input{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0);clip-path:inset(50%);white-space:nowrap}button{display:flex;align-items:center;justify-content:center;gap:9px;width:100%;min-height:52px;border:0;border-radius:12px;padding:13px 18px;background:#6f3d96;color:#fff;font:inherit;font-weight:750;box-shadow:0 8px 22px rgba(91,47,124,.2);cursor:pointer;transition:background .16s ease,transform .16s ease,box-shadow .16s ease}button svg{width:18px;height:18px;fill:none;stroke:currentColor;stroke-linecap:round;stroke-linejoin:round;stroke-width:2}button:not(:disabled):hover{background:#5e2f80;transform:translateY(-1px);box-shadow:0 10px 26px rgba(91,47,124,.25)}button:focus-visible{outline:3px solid rgba(111,61,150,.28);outline-offset:3px}button:disabled{background:#c9bdd2;box-shadow:none;cursor:not-allowed}#message{min-height:24px;margin:14px 0 0;color:#4e3c5c;font-weight:700}ul{list-style:none;margin:14px 0 0;padding:0;display:grid;gap:8px}li{padding:12px 14px;border:1px solid #e3dce9;border-radius:10px;background:#fff;font-size:14px;overflow-wrap:anywhere}li.ok{border-color:#acd5bd;background:#f1faf4;color:#285b3c}li.error{border-color:#e2b5b5;background:#fff4f4;color:#833b3b}.privacy{display:flex;align-items:flex-start;gap:10px;margin:32px 0 0;padding-top:24px;border-top:1px solid #eee8f2;color:#756d7d;font-size:13px;line-height:1.55}.privacy svg{flex:0 0 auto;width:18px;height:18px;margin-top:1px;fill:none;stroke:#72558e;stroke-linecap:round;stroke-linejoin:round;stroke-width:1.7}@media(max-width:520px){body{padding:12px}main{border-radius:18px;padding:24px 20px}.mark{width:48px;height:48px;border-radius:14px}.mark img{width:34px;height:34px}.brand{font-size:13px}.eyebrow{margin-top:26px}h1{font-size:42px}.intro{font-size:15px}.expiry{align-items:flex-start;flex-direction:column;border-radius:12px}.drop{margin-top:26px;padding:28px 18px}}@media(prefers-reduced-motion:reduce){.drop,button{transition:none}}`;

const DROP_UPLOAD_SCRIPT = `
const form=document.getElementById('form'),input=document.getElementById('files'),button=document.getElementById('submit'),message=document.getElementById('message'),results=document.getElementById('results'),drop=document.getElementById('drop'),selection=document.getElementById('selection');
const SINGLE_REQUEST_LIMIT=80*1024*1024,CHUNK_BYTES=20*1024*1024;
document.getElementById('expiry').textContent=new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short'}).format(new Date(document.getElementById('expiry').dateTime));
function updateSelection(){const files=Array.from(input.files||[]);selection.textContent=files.length===0?'No files selected':files.length===1?files[0].name:files.length+' files selected';drop.classList.toggle('has-files',files.length>0);button.disabled=files.length===0}
input.addEventListener('change',updateSelection);
for(const name of ['dragenter','dragover'])drop.addEventListener(name,e=>{e.preventDefault();drop.classList.add('is-over')});
for(const name of ['dragleave','drop'])drop.addEventListener(name,e=>{e.preventDefault();drop.classList.remove('is-over')});
drop.addEventListener('drop',e=>{if(e.dataTransfer.files.length){input.files=e.dataTransfer.files;updateSelection()}});
const uploadBase=()=>location.pathname.replace(/\\/$/,'').replace(/^\\/drop\\//,'/api/drop/')+'/uploads';
async function readResponse(response){const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.message||'Upload failed.');return payload}
function retryDelay(response){const value=response.headers.get('Retry-After');if(!value)return 1000;const seconds=Number(value);if(Number.isFinite(seconds))return Math.max(0,seconds*1000);const date=Date.parse(value);return Number.isFinite(date)?Math.max(0,date-Date.now()):1000}
async function completeUpload(sessionPath,item,file){for(;;){const response=await fetch(sessionPath+'/complete',{method:'POST',headers:{Accept:'application/json'}});if(response.status!==503)return readResponse(response);item.textContent='Finalizing '+file.name+' — waiting for upload capacity…';await new Promise(resolve=>setTimeout(resolve,retryDelay(response)))}}
async function uploadSmallFile(file,item){for(;;){const data=new FormData();data.append('file',file);const response=await fetch(uploadBase(),{method:'POST',body:data,headers:{Accept:'application/json'}});if(response.status!==503)return readResponse(response);item.textContent='Uploading '+file.name+' — waiting for upload capacity…';await new Promise(resolve=>setTimeout(resolve,retryDelay(response)))}}
async function initializeUpload(file,item,plan){for(;;){const response=await fetch(uploadBase()+'/chunks',{method:'POST',headers:{Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify(plan)});if(response.status!==429)return readResponse(response);const payload=await response.clone().json().catch(()=>({}));if(payload.error!=='upload_initialization_busy')return readResponse(response);item.textContent='Preparing '+file.name+' — waiting for upload capacity…';await new Promise(resolve=>setTimeout(resolve,retryDelay(response)))}}
async function putChunk(sessionPath,index,body,item,file,totalChunks){for(;;){const response=await fetch(sessionPath+'/'+index,{method:'PUT',headers:{Accept:'application/json','Content-Type':'application/octet-stream'},body});if(response.status!==503)return readResponse(response);item.textContent='Uploading '+file.name+' — chunk '+(index+1)+' of '+totalChunks+' waiting for capacity…';await new Promise(resolve=>setTimeout(resolve,retryDelay(response)))}}
async function uploadFile(file,item){if(file.size<=SINGLE_REQUEST_LIMIT)return uploadSmallFile(file,item);
const totalChunks=Math.ceil(file.size/CHUNK_BYTES);const initialized=await initializeUpload(file,item,{filename:file.name,contentType:file.type||'application/octet-stream',totalBytes:file.size,totalChunks,chunkBytes:CHUNK_BYTES});
const sessionPath=uploadBase()+'/chunks/'+initialized.sessionId;try{for(let index=0;index<totalChunks;index++){item.textContent='Uploading '+file.name+' — chunk '+(index+1)+' of '+totalChunks;const start=index*CHUNK_BYTES,end=Math.min(file.size,start+CHUNK_BYTES);await putChunk(sessionPath,index,file.slice(start,end),item,file,totalChunks)}return await completeUpload(sessionPath,item,file)}catch(error){void fetch(sessionPath,{method:'DELETE',headers:{Accept:'application/json'}});throw error}}
form.addEventListener('submit',async e=>{e.preventDefault();const files=Array.from(input.files||[]);if(!files.length)return;button.disabled=true;input.disabled=true;results.innerHTML='';let ok=0;
for(let i=0;i<files.length;i++){const file=files[i],item=document.createElement('li');item.textContent='Uploading '+file.name+' ('+(i+1)+' of '+files.length+')…';results.append(item);message.textContent='Uploading '+(i+1)+' of '+files.length+'…';
try{await uploadFile(file,item);item.className='ok';item.textContent='Uploaded '+file.name;ok++;}catch(error){item.className='error';item.textContent=file.name+': '+(error instanceof Error?error.message:'Upload failed.');}}
message.textContent=ok===files.length?(ok===1?'File sent successfully.':ok+' files sent successfully.'):ok+' of '+files.length+' files sent.';input.disabled=false;input.value='';updateSelection();});`;
