{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "preDeployCommand": ["TOOLS_SCHEMA_PUSH_CONFIRM=tools-platform-production pnpm --dir services/tools run db:push-postgres && pnpm --dir services/tools/publisher run db:backfill"],
    "startCommand": "pnpm --dir services/tools run start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "sleepApplication": true
  }
}
