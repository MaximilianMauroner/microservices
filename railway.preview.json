{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "preDeployCommand": ["FIELD_GUIDE_SCHEMA_PUSH_CONFIRM=field-guide-console-production pnpm --dir services/tools/field-guide run db:push-postgres && pnpm --dir services/tools/publisher run db:backfill"],
    "startCommand": "pnpm --dir services/tools run start",
    "healthcheckPath": "/live",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "sleepApplication": false
  }
}
