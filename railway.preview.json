{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "preDeployCommand": ["pnpm --dir services/tools run db:migrate && FIELD_GUIDE_SCHEMA_PUSH_CONFIRM=field-guide-console-production pnpm --dir services/tools/field-guide run db:push-postgres"],
    "startCommand": "pnpm --dir services/tools run start",
    "healthcheckPath": "/live",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "sleepApplication": false
  }
}
