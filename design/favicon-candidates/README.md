# Tools Platform favicon candidates

Generated with the built-in image generator, reviewed at favicon sizes, and refined non-destructively.

## Finalists

- `network-2-page-style.png`: recommended page-native routing loop on the warm-paper canvas.
- `network-2-fixed.png`: unified routing loop with three service junctions.
- `status-1-fixed.png`: shielded telemetry route ending in a healthy-state node.
- `tools-2-fixed.png`: modular workbench/cradle with one seated connection tab.

Each finalist uses only the repository palette: ink `#18211c`, accent `#087451`, and paper `#f3f1e9`.

Browser-ready 16, 32, and 48 pixel PNGs and 32 pixel ICO files are in `exports/`.

The `network-2-page-style` exports are the recommended favicon set for the current light Tools page. The dark-canvas finalists remain preserved for comparison.

The remaining PNGs are preserved first-pass explorations for comparison.
