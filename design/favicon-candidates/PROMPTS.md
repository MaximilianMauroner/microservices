# Final refinement prompts

Mode: built-in image generation followed by deterministic normalization to the exact three-color repository palette.

## `network-2-fixed.png`

Preserve the compact hexagonal routing loop with exactly three equal round service junctions. Remove shading, gradients, texture, glow, bevel, and shadow. Use one continuous flat accent-green route on an ink background, enlarge the three paper junction centers, and reduce the central paper hexagon so it is regular, sharp, and subordinate. Optimize the geometry for a bold, symmetric silhouette at 16 pixels. Use only `#18211c`, `#087451`, and `#f3f1e9`; no text or extra nodes.

## `status-1-fixed.png`

Preserve the centered shield, telemetry route, and healthy endpoint identity. Replace the ECG-like zigzag with one thick stepped telemetry route: horizontal start, exactly one rise to a short plateau, exactly one drop to a lower run, and one distinct circular endpoint. Use a solid ink shield on paper with an accent-green route. Optimize for 16 pixels. Use only `#18211c`, `#087451`, and `#f3f1e9`; no gradients, shadows, texture, text, or medical imagery.

## `tools-2-fixed.png`

Preserve the U-shaped workbench cradle. Fuse the disconnected interior geometry into one ink module that physically joins the base, with no floating pieces. Seat exactly one accent-green rectangular connection tab in a top-center notch. Use bold flat geometry optimized for 16 pixels. Use only `#18211c`, `#087451`, and `#f3f1e9`; no gradients, shadows, texture, text, terminal glyphs, gears, or extra pieces.
